# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Navigate to directory
        run: cd randomcat/server

      # Additional steps in the randomcat/server directory
      - name: Execute command in directory
        run: |
          echo "Hello, world!"
        # Add more commands specific to the randomcat/server directory
